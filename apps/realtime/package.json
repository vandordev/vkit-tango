{
  "name": "@repo/realtime-server",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../../.env run ../../packages/config/src/run.ts --modules base,realtime -- bun run --watch src/main.ts",
    "build": "bun build src/main.ts --outdir=dist --target=node",
    "start": "bun --env-file=../../.env run ../../packages/config/src/run.ts --modules base,realtime -- bun run dist/main.js",
    "lint": "eslint",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@repo/config": "*",
    "@repo/realtime": "*",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/bun": "latest",
    "socket.io-client": "^4.8.1",
    "typescript": "5.9.2"
  }
}
