{
  "name": "@repo/worker",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../../.env run ../../packages/config/src/run.ts --modules base,worker,storage -- bun run --watch src/main.ts",
    "build": "bun build src/main.ts --outdir=dist --target=bun",
    "start": "bun --env-file=../../.env run ../../packages/config/src/run.ts --modules base,worker,storage -- bun run dist/main.js",
    "lint": "eslint",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@repo/application": "*",
    "@repo/config": "*",
    "@repo/queue": "*",
    "@repo/storage": "*",
    "pino": "^9.7.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "typescript": "5.9.2"
  }
}
